# Round 3 checkpoint 01 — Zimmermann 1778 carrier and exact page map

Date: 2026-08-27
Status: `CHECKPOINT — PRIMARY PAGE CLUSTER NOT YET CLOSED`

## Scope

This checkpoint records a consequential carrier correction for Round 3 before the first primary page cluster is closed. It does **not** promote the p.114–115 causal locus to primary-direct yet.

## Independent public carrier established

ETH-Bibliothek / e-rara holds the three-volume set:

- E. A. W. von Zimmermann, *Geographische Geschichte des Menschen, und der allgemein verbreiteten vierfüssigen Thiere, nebst einer hieher gehörigen zoologischen Weltcharte* (Leipzig: Weygandsche Buchhandlung, 1778–1783).
- ETH shelfmark: `Rar 5828`.
- e-rara title VLID: `6943985`.
- persistent DOI: `https://doi.org/10.3931/e-rara-22336`.
- Volume 1 VLID: `6943987`.
- public-domain scan; direct JPEG and IIIF image delivery are exposed by e-rara.

This is a useful independent carrier from the already-known Google Books / Internet Archive family.

## Volume 1 structure control

The e-rara structure tree establishes:

- printed p.31 = scan leaf `[51]`, start of `Erster Abschnitt. Der Mensch`;
- printed p.130 = scan leaf `[150]`, start of `Zweiter Abschnitt. Der Hund`.

The corresponding e-rara page object IDs were directly opened:

- p.31 -> page VLID `6944039`;
- p.130 -> page VLID `6944138`.

The difference is exactly 99 in both printed pagination and page-object numbering. Therefore within this continuous run:

`page VLID = 6944008 + printed page`

and the high-value Round 3 locus maps exactly to:

- printed p.114 -> page VLID `6944122`;
- printed p.115 -> page VLID `6944123`.

This arithmetic is not being used as textual evidence; it is a retrieval map. It is anchored at both ends of the human section and removes ambiguity over scan offsets.

## First-page visual control

The p.31 page image was opened directly from e-rara. It confirms the section heading and the actor's opening framing around the human being's worldwide distribution, climate, heat/cold, food and related causes of human `Ausartungen`. This page is contextual only for the present bounded test; it does not by itself settle the p.114–115 common-origin sequence.

## Current retrieval constraint

The e-rara human-section PDF is approximately 43 MB and exceeds the current web PDF fetch ceiling. The web page exposes direct JPEG/IIIF delivery for individual image VLIDs, but the current retrieval layer does not permit constructing an unlinked arbitrary image URL from a numerically derived VLID. Internet Archive's PDF/plain-text links also redirect to mirror hosts rejected by the current web fetcher.

Accordingly:

- do **not** promote p.114–115 to `primary-direct` yet;
- modern scholarship remains locator/secondary evidence only;
- next work should use the exact VLIDs `6944122` and `6944123` through an accessible IIIF/page-image path, or an alternate carrier whose page images can be fetched directly.

## Secondary locator held strictly as locator

A recent Blumenbach study identifies Zimmermann 1778 pp.114–115 as the locus for:

1. Caucasus as geographically plausible origin because of its habitable elevation and radiating mountain systems;
2. population increase and family migration across Eurasia/America/Africa;
3. gradual transfer of humans into multiple climates;
4. climatic influence transforming populations into geographically named human forms, including Georgians, Negroes and Eskimos.

Until the scans are inspected, this remains `secondary-locator`, not actor-language evidence.

## Immediate next targets

1. inspect e-rara page VLIDs `6944122` and `6944123` directly;
2. transcribe only the causal clauses needed for the Round 3 common-origin / diffusion question;
3. inspect nearby pages for `Farbe`, `Racen`, `Varietäten`, `Grad/Grade`, `schwarz`, `weiß` and determine whether colour is descriptive, classificatory or causal;
4. write the next checkpoint immediately after the first resolved page cluster.

## Evidence discipline

- page-object arithmetic is retrieval metadata, not substantive primary evidence;
- a modern translation of p.115 is not a substitute for actor language;
- migration + climate transformation must not be inflated into hereditary fixity;
- the present checkpoint changes carrier confidence and locator precision, not the substantive Round 3 conclusion.
